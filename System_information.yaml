---
- name: Gather OS version on AIX servers
    hosts: all
    gather_facts: no
    tasks:
        - name: Get AIX OS version
            command: oslevel -s
            register: os_version

        - name: Show OS version
            debug:
                msg: "AIX OS version: {{ os_version.stdout }}"